import { Component, type ReactNode } from "react";

class Header extends Component {
  render(): ReactNode {
    return (
      <header>
        <h1>Pokemons</h1>
      </header>
    )
  }
}

export default Header;